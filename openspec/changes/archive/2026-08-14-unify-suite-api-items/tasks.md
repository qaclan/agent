## 1. Shared API-item execution function (backend refactor)

- [x] 1.1 In `web/api/services/runner_service.py`, extract the per-request sequence currently inlined in `_execute_collection`/`run_collection` (resolve auth → resolve schema-check → resolve negative-check → `run_api_request` → persist `state_updates` into `collection_vars` via `CollectionVarsRepo`) into one function, e.g. `resolve_and_run_api_item(req_row, collection_row, env_vars, state, state_path=None, include_negatives=True)`, returning the full result dict.
- [x] 1.2 Update `_execute_collection` and `run_collection` to call the new shared function instead of their inlined logic, with `include_negatives=True` — pure refactor, no behavior change for collection runs. Also folded `run_request` (ad hoc single send) onto the same shared function with `include_negatives=False`, removing a third hand-rolled copy of the same sequence. Note: `run_collection` (the CLI-driven sync path) previously did **not** persist extracted vars into `collection_vars` at all — only `_execute_collection` (the web/thread path) did. The shared function now persists for both, which is a deliberate fix (matches Decision 1's intent) but is technically a behavior change for the CLI path — flagging since the task said "pure refactor."
- [x] 1.3 Verify collection runs (ad hoc send, full collection run) behave identically after the refactor: auth resolution, schema-check, negative-check, and `collection_vars` persistence all still work as before. Verified with an isolated-`$HOME` smoke test (Flask test client + `RunnerService` called directly + a local mock HTTP server, never touching the real `~/.qaclan`): `run_request` (ad hoc) resolves bearer auth correctly; `run_collection` runs both requests, resolves `auth_type='inherit'` correctly, and now also persists the extracted token into `collection_vars` (the intentional fix noted above) — confirmed via direct DB read.

## 2. Wire suite runs to the shared function (backend)

- [x] 2.1 In `web/routes/runs.py::execute_run`'s API-item branch, load the parent `api_collections` row and call `resolve_and_run_api_item(..., include_negatives=False)` instead of calling `run_api_request` directly.
- [x] 2.2 Merge the returned `state_updates` into the suite run's own `state.json.qaclan_vars`, in addition to the `collection_vars` persistence the shared function already performs. (Pre-existing merge code was already correct — kept as-is; only its input source changed.)
- [x] 2.3 Define suite API item failure as: assertion failure, request execution error, or a breaking schema-drift verdict; wire this into the existing stop-on-fail skip logic alongside script item failures. **No new code needed** — `run_api_request`'s `status` field (`cli/api_runner.py:908,935-936,975-1014`) already folds assertion failure and breaking schema-drift into `FAILED` and request exceptions into `ERROR`; since suites call with `include_negatives=False`, negative results never factor in. The existing `api_status != "PASSED"` check already implements this exactly.
- [x] 2.4 Verify `auth_type='inherit'` on a suite API item sends the collection's configured auth end to end (manual test: suite with a login request whose auth is inherited from a collection-level bearer config). **Verified end to end** with a real repro of the originally reported failure: a suite with a `Login` (POST, extracts a token via a no-code post-extractor) and a `Summary` (GET, `auth_type='inherit'` from a collection with bearer auth `{{access_token}}`) API item, run against a local mock HTTP server that 401s without the header. Result: `Login` PASSED 200, `Summary` PASSED 200 (previously would have been 401/ERROR before this change, since `auth_type='inherit'` was never resolved in the suite path).
- [x] 2.5 Verify schema-check verdicts are produced for a suite API item when enabled, matching what a standalone collection run of the same request produces. Verified: enabled `schema_check_default='on'` on a collection, ran a suite item twice against an endpoint whose response shape changed (a field removed between runs) — first run captured the baseline and passed, second run produced a `schema_drift` with `breaking_count: 1` and the item was marked `FAILED`, and the suite run's overall status was `FAILED`.
- [x] 2.6 Verify negative testing never resolves or fires for a suite API item, even when `negative_check` is enabled on the request or its collection, and no negative verdict appears in the result. Verified: set `negative_check='on'` plus a fake enabled case on a suite item's request, ran the suite, then read the persisted `api_runs.negative_result` column directly — `NULL`, confirming negatives never fired despite being enabled.
- [x] 2.7 Verify a suite API item's extracted variable is persisted into `collection_vars` and visible afterward the same way a collection run would leave it. Verified: after the suite run in 2.4, `collection_vars` has `key='access_token', initial_value='secret123'` for that collection — read directly via SQL.

## 3. Cross-item variable state fix (backend)

- [x] 3.1 In each of `cli/script_strategies/python_strategy.py`, `javascript_strategy.py`, `javascript_test_strategy.py`, `typescript_test_strategy.py`, change the `storage_state(path=_STATE)` teardown call to read the existing `state.json`'s `qaclan_vars` (if present) and merge-write it back alongside the new `cookies`/`origins`, instead of letting Playwright's write overwrite the whole file. Syntax-verified: `python3 -m py_compile` on the 4 files, `node --check` on the extracted JS harness templates (both pass); TS template reviewed by hand (same shape, valid type annotations — no `tsc` available in this sandbox to run directly).
- [ ] 3.2 Verify with a suite run of [API item that extracts a token, script, API item that references the token] that the token is still resolvable by the second API item. **Not run** — needs a real Playwright script execution (browser + runtime), which this sandbox has no Chromium/runtime set up for. The [API item, API item] half of this chain (no script in between) was verified end to end in 2.4/2.7; the script-interleaving half is verified only by code review of the merge-write logic (Decision 4) and syntax checks (Task 3.1's note) — flagging as the one piece of this change without a real execution test.

## 4. Mixed-item reorder endpoint (backend)

- [x] 4.1 Replace `PUT /suites/<id>/order`'s `script_ids`-only contract in `web/routes/suites.py` with one accepting an ordered list of `suite_items.id` values covering every item in the suite, and update `order_index` for each accordingly. New body key: `item_ids`.
- [x] 4.2 Reject (400) if the submitted id list doesn't match the suite's current item set exactly (missing or extra ids). Also rejects on duplicates (length check, not just set equality).

## 5. Suite editor: unify item list (frontend)

- [x] 5.1 In `web/static/app.js::editSuiteModal`, remove the legacy "Scripts" section and its dedicated rendering/drag handlers.
- [x] 5.2 Make the existing "Items" list (script + API rows) drag-and-drop reorderable, calling the new endpoint from Task 4.1 with the resulting item-id order.
- [x] 5.3 Route script-item removal through the same removal path API items use (`removeSuiteItem` / `DELETE /suites/<id>/items/<item_id>`), retiring the separate `removeSuiteScript` call from the editor. The now-fully-unused `removeSuiteScript` JS function was deleted (zero remaining callers); the backend `DELETE /suites/<id>/scripts/<script_id>` route was left in place — deprecating a REST endpoint outside this task's stated scope.
- [x] 5.4 Update `list_suites`' `script_count` column name/label in `web/routes/suites.py` and its frontend usage to reflect that it counts all items, not just scripts. Renamed to `item_count` / "N items". (A same-named `script_count` on the unrelated Features page was left untouched — different query, different page.)

## 6. Bulk API request picker (frontend)

- [x] 6.1 Replace `addApiRequestToSuite`'s single-select dropdown with a full-height modal listing API requests grouped by collection (fetch collections + requests, group client-side), each request row with a checkbox. Used the `modal-lg` size class (92vh) plus new `.api-picker-*` CSS; also repointed the old `.suite-script-*` CSS (now dead after Task 5.1) onto `.suite-item-*` so the merged list keeps the nicer row/drag/order styling instead of the old bare-bones Items rendering.
- [x] 6.2 Add a search/filter input that filters visible rows by name, method, or URL across all collections.
- [x] 6.3 On confirm, add all checked requests to the suite in one operation (loop of `POST /suites/<id>/items` calls), appended in check order (`Set` insertion order).
- [ ] 6.4 Verify a user can check requests from two different collections in one picker session and both get added. **Not interactively tested** — no browser available in this sandbox to click through the picker UI. Code-reviewed instead: `checkedIds` is one `Set` shared across all collection groups in `renderList`, and the "Add" handler loops over the whole set regardless of which group a request came from, so nothing in the implementation scopes selection to one collection.

## 7. Suite run results: expand-in-place API detail (frontend)

- [x] 7.1 In `web/static/app.js::showRunResults`, add an expand control to each API item row that reveals status, headers, body, assertions, and schema-drift verdict. `schema-diff-view.js` is already loaded globally as a classic script before `app.js` (`web/static/index.html:32`, its own docstring names "the Runs history modal (app.js)" as an intended consumer) — called `window.qcSchemaDriftPill`/`window.qcSchemaDiffHtml` directly, no dynamic import needed. Assertion list and response-body/headers panels hand-rolled inline (collection-run-view.js's version lives inside a private IIFE closure, not exported for external reuse) using new `.api-result-*` CSS, same visual language.
- [x] 7.2 Confirm no negative-testing section/pill ever renders for a suite API item (negatives never run there). By construction: `window.qcNegativePill` is never called anywhere in this code path.
- [ ] 7.3 Verify the expanded panel's fidelity (status, headers, body, assertions, schema diff) matches what the API section's own response panel shows for the same captured result. **Not interactively tested** — no browser available. Data-level fidelity confirmed instead: the same `api_runs` fields (`response_body`, `response_headers`, `assertion_results`, `schema_drift`) that back the API section's response panel are exactly what `get_run`'s merge (Task 8.1) feeds this panel, and the same `window.qcSchemaDriftPill`/`qcSchemaDiffHtml` renderer is called in both places — verified present and correctly loaded (`web/static/index.html:32`), not visually verified.

## 8. Suite run history and report: include API items (backend)

- [x] 8.1 In `get_run` (`web/routes/runs.py:145`), call `ApiRunRepo().list_by_suite_run(run_id)`, normalize each row to the `item_type: 'api_request'` shape the live in-memory response already uses (map `request_name` → `name`), concatenate with the `script_runs` rows, and sort the merged list by `order_index` before assigning to `run["scripts"]`.
- [x] 8.2 In `cli/report.py::generate_html_report`, apply the same merge against the `scripts` query, and add a sibling `_render_api_item(item)` next to `_render_script` (status badge, method+URL, status code, duration, assertion pass/fail count, schema-drift badge if present, response body in a `<details>` block) — call it in the render loop wherever an item's `item_type` is `'api_request'`. Went further than hand-rolling this: `cli/api_report.py` already has `_render_body`/`_render_assertions`/`_render_headers_table`/`_render_schema_drift` purpose-built for exactly this (its docstring says the two reports are meant to mirror each other), so `_render_api_item` imports and reuses those directly instead of duplicating them — same visual language as `_render_script`'s card, same GitHub-style color palette both report modules already share. Confirmed `cli/ → web.api.*` imports are an established pattern here (`cli/commands/api_cmd.py` already does this extensively) before adding the `ApiRunRepo` import to `cli/report.py`.
- [x] 8.3 Verify reopening a past suite run's execution history (Runs list → view) shows every API item as a row, matching the header total, in original suite order. Verified via `GET /api/runs/<id>` against the Flask test client (Werkzeug's routing/view layer, same code the browser hits): both items present, `total=2` matches the row count, order matches suite order.
- [x] 8.4 Verify the downloaded HTML report for that same run includes a card per API item, in original suite order, alongside script cards. Verified via `GET /api/runs/<id>/report`: both item names present in the returned HTML. **This caught a real bug** — `_render_api_item` initially read `item.get("name")`, but `ApiRunRepo.list_by_suite_run` returns the joined column as `request_name`, so every API card silently showed the raw `api_request_id` instead of its name. Fixed (now reads `request_name`) and re-verified.

## 9. Manual verification

- [x] 9.1 Recreate the original reported failure (suite with login → summary API items, summary depending on login's bearer token) and confirm the suite run now succeeds end to end. Done — this is exactly Task 2.4's verification; the originally reported repro now passes.
- [ ] 9.2 Regression-check a suite with only scripts (no API items) still edits, reorders, and runs exactly as before. **Not run** — needs Playwright/browser execution, unavailable in this sandbox (same limitation as Task 3.2). The editor/reorder-endpoint code paths for a script-only suite were code-reviewed: `editSuiteModal`'s unified list and the reorder endpoint's `item_ids` contract both treat a script item exactly like an API item (same `suite_items.id` key, same drag handlers, same removal path) — no script-specific branch was removed, only the duplicate rendering was.
- [x] 9.3 Regression-check a standalone collection run (ad hoc send and full collection run) still resolves auth/schema-check/negative-check and persists variables exactly as before the refactor. Same verification as Task 1.3.

## 10. Suite item row polish: right-aligned actions, confirm-before-remove, API item View (frontend)

- [x] 10.1 In `web/static/app.js::editSuiteModal`, wrap each row's View/Remove buttons in a `.suite-item-actions` span (new CSS: `margin-left:auto`) so they sit flush right regardless of item name/URL length, for both script and API rows. Root cause of the "duplicate" text (found from a follow-up screenshot): `item.api_request_name` already includes the method (e.g. `"POST /api/v1/user/login"`), so prepending `item.method` a second time produced `"POST POST /api/v1/user/login"` — removed the prepend entirely, name renders as stored. Root cause of the misaligned/cut-off last row: the name+URL spans had no shrink/truncation, so a long URL pushed the row wider than the modal and clipped the trailing Remove button off-screen — fixed by wrapping name+URL in `.suite-item-info` (`flex:1;min-width:0`) with the URL span (`.suite-item-url`) ellipsis-truncating (`overflow:hidden;text-overflow:ellipsis;white-space:nowrap`), so `.suite-item-actions` always has room and stays visible at the row's right edge.
- [x] 10.2 Swap `removeSuiteItem`'s native `confirm()` for the app's styled `window._confirmDialog(...)` (same dialog used everywhere else in the API section), so removal always requires an explicit confirm click, not a raw browser dialog.
- [x] 10.3 Add a View button to API-request rows (previously script-only). New `viewApiRequestInEditor(requestId)` closes the suite modal, stashes the id on `window._qcApiInitialRequestId`, and calls `navigate('api')`.
- [x] 10.4 Thread an optional `initialRequestId` through `routes.api` → `window.__qaclanApi.render(container, initialRequestId)` → `renderApiPage(container, initialRequestId)` (`web/static/api/api-section.js`). When set, after the collections sidebar wires up, it calls the existing `refresh(requestId)` (highlights the row in the sidebar tree) and `renderRequestEditor(mainEl(), requestId)` — the exact same editor `View`/row-click already opens from inside a collection, with `collection_id` self-resolved from the fetched request (`request-editor-view.js`'s `_effectiveCollectionId`), so no collection/env context needs threading through.
- [x] 10.5 Syntax-verified: `node --check web/static/app.js` and `node --check web/static/api/api-section.js` both pass. Not interactively browser-tested (no browser in this sandbox) — same limitation as 6.4/7.3.

## 11. API request picker polish: fixed height, collapsible groups, live selected count (frontend)

- [x] 11.1 In `web/static/app.js::addApiRequestToSuite`, give `#api-picker-list` its own fixed height + `overflow-y:auto` (new `.api-picker-list` CSS: `height:55vh`) so typing in the search box only changes which rows are visible inside that fixed region — it no longer shrinks/grows the whole modal as the filtered result count changes.
- [x] 11.2 Make each collection group collapsible: header (chevron + label + count) is now a separate `.api-picker-group-header` above a `.api-picker-group-body` wrapping its rows; clicking the header toggles a session-only `collapsedGroups` Set (nothing persisted) and re-renders. While a search term is active, a group with a match always renders expanded regardless of its collapsed state, so filtered results are never hidden.
- [x] 11.3 Add a live "N selected" count to each group header, computed at render time as `reqs.filter(r => checkedIds.has(r.id)).length` against the picker's own in-memory `checkedIds` Set — no new state, no persistence, matches the explicit instruction not to remember selection across sessions.
- [x] 11.4 Checkbox toggling now also re-renders (to refresh the live count), so scroll position is explicitly saved/restored around every re-render (`_rerenderPickerList`) to avoid jumping the list back to top on every click.
- [x] 11.5 Syntax-verified: `node --check web/static/app.js` passes. Not interactively browser-tested (no browser in this sandbox) — same limitation as 6.4/7.3/10.5.

### Verification method note
Tasks marked done above with a description were verified via an isolated-`$HOME` smoke test (a local mock HTTP server + Flask's test client + direct SQL reads), never touching the real `~/.qaclan` database on this machine. No automated test suite exists in this repo (per CLAUDE.md) to leave these as permanent regression tests; they were throwaway scripts run to confirm behavior, not committed. Two items (3.2, 9.2) need an actual Playwright browser run to fully verify and were not — this sandbox has no Chromium/runtime installed. 6.4 and 7.3 need interactive browser UI clicking, not available here either; both were code-reviewed instead.
